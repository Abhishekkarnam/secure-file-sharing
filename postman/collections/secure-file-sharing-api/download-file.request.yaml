$kind: http-request
name: 'Download File'
method: GET
url: '{{base_url}}/files/download/{{filename}}'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
pathVariables:
  - key: filename
    value: 'sample.txt'
